apiVersion: v1
kind: Service

metadata:
  name: payload-service
  namespace: depi4ops

spec:
  type: NodePort

  selector:
    app: payload

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080